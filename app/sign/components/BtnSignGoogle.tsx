import { btnOutlineClasses } from '@/styles/commonClasses';

export default function BtnSignGoogle() {
   return (
      <section>
         <button className={btnOutlineClasses}>Continue with Google</button>
      </section>
   );
}
